# Python environments and caches.
# The glob matters: candidate-evaluation environments such as .venv-transport-bench are
# hundreds of megabytes and must not be committable by accident.
.venv/
.venv-*/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Everything under artifacts/ is regenerated by the just recipes
# (synthetic-calibration, test-e2e, playwright-cli). Keeping it out of the
# repository avoids shipping stale evidence from another machine.
artifacts/

# Measured hardware profiles are machine specific.
config/hardware_profile.measured.json

# Personal face models and the profile paths that reference them are biometric data.
# Keep the checked-in demo profile only; store real shape.pcd files outside this repository.
config/user_profile.measured.json
config/private/

# Model weights are fetched on first run by the FaceMesh component.
models/

# Scratch space.
temp/

# Editor and OS state.
.vscode/
.idea/
.serena/
.DS_Store
Thumbs.db

# Working documents are never published. They quote the operator, name local paths,
# and record intermediate findings that are wrong on purpose (superseded hypotheses).
# temp/ is already excluded above; these cover the other places such notes land.
diary/
workdocs/
notes/
scratch/
**/workdoc_*.md
**/作業*.md

# Raw capture from the operator's own machine. Only the converted, reviewed demo
# under docs/assets/ is tracked.
*.mov
# Large tomato smoke point-cloud assets (regenerated; not to be committed).
src/headcoupled_display/static/assets/tomato_*.pcd
config/scene_tomato_*.json
